import React, { useEffect, useState } from 'react';
import { Navigate } from 'react-router-dom';
import { PageHeader } from '../components/layout/PageHeader';
import { Card, CardContent, CardHeader, CardTitle } from '../components/ui/Card';
import { useAuth } from '../context/AuthContext';
import { getSupportOfficeSnapshot, listSupportAccessRequests, requestSupportAccess, type SupportAccessRequest } from '../services/supabaseSupportAccess';
import { getPlatformMetrics, listOperationsInbox, listPlatformOffices, setPlatformOfficeStatus, updateOperationsInboxItem, type OperationsInboxItem, type PlatformMetrics } from '../services/supabaseAdmin';
import { subscriptionLabel } from '../lib/access';
import type { Office, SubscriptionStatus } from '../types';

export const Admin = () => {
  const { user } = useAuth();
  const [offices, setOffices] = useState<Office[]>([]);
  const [accessRequests, setAccessRequests] = useState<SupportAccessRequest[]>([]);
  const [snapshot, setSnapshot] = useState<Record<string, unknown> | null>(null);
  const [privacyRequests, setPrivacyRequests] = useState<OperationsInboxItem[]>([]); const [supportTickets, setSupportTickets] = useState<OperationsInboxItem[]>([]);
  const [integrationErrors, setIntegrationErrors] = useState<OperationsInboxItem[]>([]); const [metrics, setMetrics] = useState<PlatformMetrics | null>(null);
  const refresh = async () => { const [officeResult, requests, inbox, metricResult] = await Promise.all([listPlatformOffices(), listSupportAccessRequests(), listOperationsInbox(), getPlatformMetrics()]); setOffices(officeResult.offices); setAccessRequests(requests); setPrivacyRequests(inbox.privacyRequests); setSupportTickets(inbox.supportTickets); setIntegrationErrors(inbox.integrationErrors); setMetrics(metricResult); };
  useEffect(() => { if (user?.globalRole === 'platform_admin') refresh().catch(console.error); }, [user?.globalRole]);
  if (user?.globalRole !== 'platform_admin') return <Navigate to="/" replace />;
  const updateStatus = async (officeId: string, status: SubscriptionStatus) => { await setPlatformOfficeStatus(officeId, status); setOffices(items => items.map(item => item.id === officeId ? { ...item, subscriptionStatus: status } : item)); };
  const inboxCard = (title: string, kind: 'privacy' | 'support', items: OperationsInboxItem[]) => <Card><CardHeader><CardTitle>{title} ({items.filter(item => !['resolved', 'closed'].includes(item.status)).length})</CardTitle></CardHeader><CardContent className="space-y-3">{items.slice(0, 20).map(item => <div key={item.id} className="rounded-lg border border-slate-200 p-4 text-sm"><div className="flex flex-col justify-between gap-3 md:flex-row"><div><p className="font-semibold text-slate-900">{item.subject || item.requestType || item.id}</p><p className="mt-1 text-slate-600">{item.message || item.details}</p><p className="mt-2 text-xs text-slate-500">{item.email || 'Usuário autenticado'} · {item.status}</p></div><select className="h-9 rounded-md border border-slate-300 px-2" value={item.status} onChange={async event => { await updateOperationsInboxItem(kind, item.id, event.target.value); await refresh(); }}>{['open', 'in_progress', 'resolved', 'closed'].map(status => <option key={status} value={status}>{status}</option>)}</select></div></div>)}</CardContent></Card>;
  return <div className="mx-auto max-w-6xl space-y-6 pb-12"><PageHeader title="Administração global" description="Visão operacional protegida para suporte e gestão da base SaaS." breadcrumbItems={[{ label: 'Administração global' }]} />{metrics && <div className="grid gap-3 sm:grid-cols-2 lg:grid-cols-4">{[['Escritórios', metrics.totalOffices], ['Ativação', `${metrics.activationRate}%`], ['Conversão', `${metrics.conversionRate}%`], ['MRR estimado', metrics.mrr.toLocaleString('pt-BR', { style: 'currency', currency: 'BRL' })]].map(([label, value]) => <Card key={label}><CardContent className="p-5"><p className="text-xs font-semibold uppercase text-slate-500">{label}</p><p className="mt-2 text-2xl font-bold text-slate-900">{value}</p></CardContent></Card>)}</div>}<Card><CardHeader><CardTitle>Escritórios ({offices.length})</CardTitle></CardHeader><CardContent className="space-y-3">{offices.map(office => { const approved = accessRequests.find(item => item.officeId === office.id && item.status === 'approved' && Number(item.expiresAtMs || 0) > Date.now()); return <div key={office.id} className="grid gap-3 rounded-lg border border-slate-200 p-4 md:grid-cols-[1fr_160px_170px_170px] md:items-center"><div><p className="font-semibold text-slate-900">{office.name}</p><p className="text-xs text-slate-500">{office.email} · {office.planCode || 'trial'}</p></div><span className="text-sm text-slate-600">{office.subscriptionStatus ? subscriptionLabel[office.subscriptionStatus] : 'Sem status'}</span><select className="h-9 rounded-md border border-slate-300 px-2 text-sm" value={office.subscriptionStatus || 'TRIALING'} onChange={event => updateStatus(office.id, event.target.value as SubscriptionStatus)}><option value="TRIALING">Em teste</option><option value="ACTIVE">Ativo</option><option value="PAST_DUE">Pendente</option><option value="GRACE_PERIOD">Tolerância</option><option value="SUSPENDED">Suspenso</option><option value="CANCELED">Cancelado</option></select>{approved ? <button className="text-sm font-semibold text-brand-700" onClick={async () => setSnapshot(await getSupportOfficeSnapshot(approved.id) as unknown as Record<string, unknown>)}>Diagnóstico autorizado</button> : <button className="text-sm font-semibold text-slate-600" onClick={async () => { const reason = window.prompt('Descreva o motivo técnico do acesso:'); if (reason && reason.length >= 10) { await requestSupportAccess(office.id, reason); await refresh(); } }}>Solicitar acesso</button>}</div>; })}{offices.length === 0 && <p className="text-sm text-slate-500">Nenhum escritório retornado ou Functions ainda não publicadas.</p>}</CardContent></Card>{inboxCard('Solicitações LGPD', 'privacy', privacyRequests)}{inboxCard('Chamados de suporte', 'support', supportTickets)}{integrationErrors.length > 0 && <Card><CardHeader><CardTitle>Erros de integração ({integrationErrors.length})</CardTitle></CardHeader><CardContent><pre className="max-h-72 overflow-auto rounded-lg bg-slate-950 p-4 text-xs text-red-200">{JSON.stringify(integrationErrors, null, 2)}</pre></CardContent></Card>}{snapshot && <Card><CardHeader><CardTitle>Diagnóstico autorizado</CardTitle></CardHeader><CardContent><pre className="max-h-96 overflow-auto rounded-lg bg-slate-950 p-4 text-xs text-slate-100">{JSON.stringify(snapshot, null, 2)}</pre></CardContent></Card>}</div>;
};
